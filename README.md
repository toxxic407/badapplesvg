# Bad Apple SVG
Bad Apple but its an SVG Animation

the bash script installs the dependencies (yt-dlp, ffmpeg and potrace), downloads the yt-video, extracts frames, converts those to svg and then splices them together to a single svg animation
